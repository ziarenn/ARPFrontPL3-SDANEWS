import React from "react";

const Navbar = () => {
  return (
    <nav>
      <h1>SDA NEWS</h1>
      <button>Home</button>
      <button>Search</button>
      <button>Login</button>
    </nav>
  );
};

export default Navbar;
