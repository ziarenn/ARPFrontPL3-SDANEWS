import React from "react";

const Form = () => {
  return (
    <form>
      <input type="text" placeholder="Keyword" />
      <button type="submit">Search</button>
    </form>
  );
};

export default Form;
